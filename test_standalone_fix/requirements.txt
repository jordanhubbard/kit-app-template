# Core Python dependencies for Kit App Template
# Required for template system and build tools

# TOML support (choose based on Python version)
# Python 3.11+ has tomllib built-in
# For Python 3.10 and below, we need the toml package
toml>=0.10.2; python_version < '3.11'

# Optional but recommended for better TOML support
tomli>=2.0.0; python_version < '3.11'
tomli-w>=1.0.0
